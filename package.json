{
  "name": "minicsv-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "Tauri desktop wrapper for the offline miniCSV editor.",
  "author": "Lyncius LLC",
  "type": "module",
  "scripts": {
    "check:editor": "node scripts/check-editor.mjs",
    "dev": "tauri dev",
    "build": "tauri build",
    "build:mas": "tauri build --config src-tauri/tauri.mas.conf.json --bundles app --target universal-apple-darwin",
    "check": "npm run check:editor && cargo check --manifest-path src-tauri/Cargo.toml"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.1"
  }
}
